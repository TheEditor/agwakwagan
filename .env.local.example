# Agwakwagan API Configuration
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control

# ============================================================================
# API Authentication Keys
# ============================================================================
# Format: {SERVICE}_API_KEY=<long-random-key>
#
# Generate secure keys:
#   Linux/Mac: openssl rand -base64 32
#   PowerShell: [Convert]::ToBase64String((1..32|ForEach-Object{Get-Random -Maximum 256}))
#
# Security Notes:
# - Keys should be at least 32 characters long
# - Use different keys per service/actor
# - Store in secure secret manager for production (AWS Secrets Manager, Vault, etc.)
# - Never share or commit these keys
#
# Key Scoping:
# - '*' = access to all boards
# - 'board-id' = access to specific board only
#

# Beads integration - access to all boards
BEADS_API_KEY=beads-key-change-me-to-secure-token

# CI/CD pipeline - access to specific board
CI_API_KEY=ci-key-change-me-to-secure-token

# Uncomment to add more keys:
# AGENT_API_KEY=agent-key-change-me-to-secure-token
# WEBHOOK_API_KEY=webhook-key-change-me-to-secure-token
